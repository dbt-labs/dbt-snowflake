version: 2

models:
  - name: lowercase
    columns:
      - name: id
        quote: true
        tests:
          - unique
  - name: uppercase
    columns:
      - name: id
        quote: true
        tests:
          - unique
