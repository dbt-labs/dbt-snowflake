version: 2
models:
  - name: outer
    description: The outer table
    columns:
      - name: id
        description: The id value
        tests:
          - unique
          - not_null

sources:
  - name: my_source
    tables:
      - name: my_table
