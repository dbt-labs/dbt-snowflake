[tox]
skipsdist = True
envlist = py36,py37,py38,py39,flake8

[testenv:flake8]
description = flake8 code checks
basepython = python3.8
skip_install = true
commands = flake8 --select=E,W,F --ignore=W504,E741 --max-line-length 99 \
  dbt
deps =
  -rdev_requirements.txt

[testenv:{unit,py36,py37,py38,py39,py}]
description = unit testing
skip_install = true
passenv = DBT_* PYTEST_ADDOPTS
commands = {envpython} -m pytest {posargs} tests/unit
deps =
  -rdev_requirements.txt
  -e.

[testenv:{integration,py36,py37,py38,py39,py}-{snowflake}]
description = adapter plugin integration testing
skip_install = true
passenv = DBT_* SNOWFLAKE_TEST_* PYTEST_ADDOPTS
commands =
  snowflake: {envpython} -m pytest {posargs} -m profile_snowflake tests/integration
deps =
  -rdev_requirements.txt
  -e.

[pytest]
env_files =
    test.env
testpaths =
    tests/unit
    tests/integration
